{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^0.14.0",
    "react-devtools-inline": "^4.0.0-alpha.7",
    "react-dom": "^0.0.0-375616788",
    "react-scripts": "3.0.1",
    "scheduler": "^0.0.0-375616788"
  },
  "scripts": {
    "start": "PORT=3002 react-scripts start",
    "build": "react-scripts build",
    "deploy": "yarn build && now deploy --static && now alias",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
